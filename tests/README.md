# Tests

This repository currently relies on integration-style validation using Docker and
live Kong requests.

Planned direction:
- Add Busted-based unit tests under `tests/spec/`.
- Add CI workflow for lint and integration verification.
